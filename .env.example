# Optional: path override for SQLite DB (default: data/declassified.db)
# DATABASE_PATH=./data/declassified.db

# Force read-only JSON feed (no SQLite, drafts disabled). Set automatically on Vercel.
# READ_ONLY=1

# Optional future: NARA Catalog API key
# NARA_API_KEY=
